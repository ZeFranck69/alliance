<div class='header-content main-wrapper'>
    <div class="header__logo-wrapper">
        <a href="{{ site.url }}" class="logo--sticky">
            {{ logo }}
        </a>
    </div>
    <div class="header__logo-wrapper--alternate">
        <a href="{{ site.url }}">
            <img src="{{ options.logo_alternatif.url }}" alt="{{ options.logo_alternatif.alt }}"> 
        </a>
    </div>
    <div class='header--menu-container'>
        <nav class='nav__links'>
            {% include 'partial/menu.twig' with {'items': menu_header.get_items } %}
        </nav>
    </div>
</div>

<div class="header--mobile">
    <div class="main-logo--mobile">
        <!-- <a class='logo--link' href='{{ site.url }}'>
            <img src="{{ options.logo_alternatif.url }}" alt="{{ options.logo_alternatif.alt }}"> 
            <!-- <img src="{{ options.footer_logo.url }}" alt="{{ options.footer_logo.alt }}"> 
        </a> -->
    </div>

    <div class="site__burger-menu">
        <div class="burger-menu__wrapper">
            <span class="burger-menu__line"></span>
            <span class="burger-menu__line"></span>
            <span class="burger-menu__line"></span>
            
        </div>
    </div>

    <script>
        // Start midnight
        jQuery(document).ready(function(){
            // Change this to the correct selector for your nav.
            jQuery('#site-header .header--mobile').midnight();
        });
    </script>
</div>
